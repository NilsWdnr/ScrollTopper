'use strict';

(() => {

// === DOM & VARS ===
const DOM = {};

// === INIT =========

const init = () => {
    new ScrollTopper({
        scrollHeight: 0
    });
}

// === EVENTS & XHR =======

// === FUNCTIONS ====

init();

})();