'use strict';

(() => {

// === DOM & VARS ===
const DOM = {};

// === INIT =========

const init = () => {
    new ScrollTopper();
}

// === EVENTS & XHR =======

// === FUNCTIONS ====

init();

})();