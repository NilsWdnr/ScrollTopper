'use strict';

(() => {

// === DOM & VARS ===
const DOM = {};

// === INIT =========

const init = () => {
    new ScrollTopper({
        scrollHeight: 0,
        width: 80,
        backgroundColor: "blue"
    });
}

// === EVENTS & XHR =======

// === FUNCTIONS ====

init();

})();